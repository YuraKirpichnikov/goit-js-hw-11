import { defineConfig } from 'vite';

// Replace "goit-js-hw-11" below with your actual repository name
// if you rename the repo, so GitHub Pages can resolve asset paths.
export default defineConfig({
  base: '/goit-js-hw-11/',
});
